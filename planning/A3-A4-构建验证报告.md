# A3/A4 产出：桌面样包构建与验证报告

> 依据：`planning/桌面客户端化方案.md` 阶段 A 的 A3（样包构建与验证）、A4（记录体积/启动/阻塞）。
> 验证环境：macOS 26.5.2 / arm64；Python 3.12.13；轻量依赖集合（无 torch/funasr/modelscope/sentence-transformers）。
> 记录日期：2026-09-09。**所有数据为本机实测，非估算。**

---

## 1. 结论摘要

| 验证项 | 结果 |
|--------|------|
| macOS arm64 样包构建 | ✅ 产出 `VideoDevour.app`（367MB 未压缩） |
| 冻结后端启动 | ✅ 握手 ~0.9s，服务就绪 ~0.95s（3 次实测） |
| 端到端任务（在线 ASR 图文报告） | ✅ 完整跑通：4 章大纲 + 47 关键帧 + 4 份报告 |
| 捆绑 ffmpeg 生效 | ✅ PATH 限制为 `/usr/bin:/bin`（无 ffmpeg）下仍完成切片与抽帧 |
| 同源前端托管 | ✅ 根路由/SPA 深层路由/静态资源/404 边界全部正确 |
| 单实例锁 | ✅ 文件锁实现（非进程名匹配） |
| 进程回收 | ✅ 壳被 SIGKILL 后，后端 8s 内自行退出（父进程监测） |
| 窗口真实打开 | ✅ CoreGraphics 确认窗口存在，1280×860 |
| Windows x64 | ✅ | CI（windows-latest）构建 + 冒烟测试通过；**未在真机做完整业务链路验证** |

**阶段 A 出口条件评估**：两平台样包均由 CI 在干净环境构建成功并通过启动/health 冒烟测试，macOS 侧另完成端到端业务链路验证。**Windows 业务链路仍需真机确认。**

### 1.1 可测试交付包

| 文件 | 平台 | 大小 | 来源 |
|------|------|------|------|
| `desktop/release/VideoDevour-macos-arm64.zip` | macOS arm64 | 192MB | 本机构建 |
| `desktop/release/VideoDevour-windows-x64.zip` | Windows x64 | 200MB | CI run 34359909564 |

测试说明见 `desktop/release/README-测试说明.md`。

### 1.2 CI 验证记录（2026-09-09）

| run | 分支 | macOS arm64 | Windows x64 |
|-----|------|-------------|-------------|
| 34359068149 | feat/desktop-client | ✅ | ❌ 打包步骤失败（PowerShell 编码） |
| 34359909564 | feat/desktop-client | ✅ | ✅ |

失败原因：`build_win.ps1` 为无 BOM 的 UTF-8，Windows runner 的 PowerShell 5.1 按 ANSI 读取，
中文注释被解析为乱码导致语法错误。修复：`.ps1` 与 `.iss` 均加 UTF-8 BOM。

---

## 2. 产物结构

```
desktop/dist/VideoDevour.app/                 # 367MB
└── Contents/
    ├── Info.plist
    ├── MacOS/VideoDevour                     # 启动脚本（exec Resources/VideoDevour）
    └── Resources/                            # PyInstaller onedir 产物
        ├── VideoDevour                       # 单可执行文件，双角色
        ├── _internal/
        │   ├── bin/ffmpeg, ffprobe           # 86MB（arm64 静态构建）
        │   ├── frontend/dist/                # 4.4MB 前端产物
        │   ├── camel/ tiktoken_ext/ yt_dlp/  # 运行时依赖
        │   └── ...
```

### 单可执行文件双角色设计

```
VideoDevour                  → 桌面壳：派生自身 --backend-only + 开窗口
VideoDevour --backend-only   → 后端：uvicorn 托管 API + 前端
```

原因：.app 内查找第二个 EXE 会引入路径与签名复杂度；`sys.executable` 派生自身最稳。

---

## 3. 实测数据（A4 基线）

| 指标 | 实测值 | 方案暂定目标 | 判定 |
|------|--------|-------------|------|
| 未压缩体积 | 367MB | ≤ 500MB | ✅ |
| 压缩后（tar.gz） | 155MB | — | 记录 |
| 其中捆绑 ffmpeg | 86MB | — | 可优化（见 §6） |
| 启动到握手 | 861 / 863 / 921ms | — | 记录 |
| 启动到服务就绪 | 889 / 890 / 952ms | ≤ 3s | ✅ |
| 空闲内存（RSS） | 未测 | 阶段 A 后定预算 | ⏳ 待补 |
| 端到端任务耗时 | ~90s（60s 视频） | — | 记录 |

---

## 4. 打包过程中发现并修复的坑（重要）

| # | 问题 | 现象 | 修复 |
|---|------|------|------|
| 1 | `console=False` 导致静默退出 | macOS 窗口模式用 `runw` 引导器，不保留标准文件描述符，uvicorn 启动后无任何日志即退出 | spec 改 `console=True`；Windows 侧由壳传 `CREATE_NO_WINDOW` 隐藏控制台 |
| 2 | stdout 握手在窗口模式不可用 | 壳读不到端口 | 改为握手**文件**（原子替换写入）+ stdout 双通道 |
| 3 | `tiktoken_ext` 未收集 | 任务在 LLM 调用处报 `Unknown encoding o200k_base`，任务失败 | spec 用 `find_spec` 定位命名空间包目录并作为 datas 收集（`collect_data_files` 无效） |
| 4 | `camel.runtimes` 收集告警 | 缺 tqdm（仅 Docker runtime 用） | 加入 `requirements-lite.txt` |
| 5 | 关闭壳后后端成孤儿 | 残留进程继续占用端口 | 壳装信号处理器 + **后端监测父进程**（壳被 SIGKILL 时也能退出） |
| 6 | 端口竞争 | 方案明确禁止"探测→关闭→再绑定" | 绑定 socket 后直接交给 `uvicorn.Server.run(sockets=[sock])` |

### 4.1 真机测试暴露的缺陷（2026-09-09 补充）

macOS 测试包首次实测时报告 `ffmpeg is not installed`，定位出 4 个真实缺陷：

| # | 问题 | 现象 | 修复 |
|---|------|------|------|
| 7 | `_get_ydl` 未设 `ffmpeg_location` | yt-dlp 合并音视频时只查系统 PATH，**忽略捆绑的 ffmpeg**，报 `You have requested merging of multiple formats but ffmpeg is not installed` | `_get_ydl` 传入 `ffmpeg_location=ffmpeg_path()`；yt-dlp 会据此自动推导 ffprobe（含 Windows `.exe`）。已用真实 B站链接验证合并成功 |
| 8 | 父进程监测误杀独立启动 | 后台启动后端时 shell 立刻退出，`os.getppid()` 变化触发"父进程已退出"而自行终止 | 改为壳通过 `--parent-pid` **显式传入 PID** 才启用监测；独立启动/CI 不监测 |
| 9 | 轻量包默认离线 ASR | 新用户无 `settings.json` 时默认 `offline`，但包内无 torch，一上传视频必然失败 | `DEFAULT_SETTINGS.asr_mode` 改为 `online`（方案 3.1 要求）；已有配置不被静默改写 |
| 10 | 离线模式裸异常 | 报 `No module named 'torch'`，用户无法据此操作 | `asr_factory` 捕获 ImportError 并提示"切换在线 ASR 或安装增强版" |
| 11 | provider/model 串台 | 切换在线 ASR 提供商后残留旧模型名，报 `Model not found` | 新增 `PROVIDER_DEFAULT_ASR_MODEL` 常量；切 provider 且未显式指定模型时自动跟随 |

**验证**：修复后 macOS 冻结包在 PATH 限制为 `/usr/bin:/bin`（无 ffmpeg）下，完整跑通
B站链接 → 下载合并 → 在线 ASR → 关键帧 → 报告（产物含 26 张关键帧，状态 completed）。

### 4.2 同步主干新功能时的打包缺口（2026-09-11）

主干的 PDF 导出（`backend/algorithm/pdf_export.py`）在 API 中通过**函数内导入**调用，
PyInstaller 静态分析无法发现，导致 `reportlab` 整个包未被收集；冻结包内 `/api/export?fmt=pdf`
会抛 `ModuleNotFoundError: reportlab`。

| # | 问题 | 修复 |
|---|------|------|
| 12 | 函数内导入的模块其依赖被漏收集（reportlab） | spec 显式 `collect_submodules("reportlab")` + `collect_data_files("reportlab")` |

> **通用教训**：凡是在函数体内 `import` 的第三方包，PyInstaller 都可能漏收集；
> 新增此类依赖时必须同步更新 spec 的 hiddenimports。本次已验证的补齐清单见 spec 注释。

**同步后冻结包验证**（macOS，PATH 限制、无 torch）：

| 功能 | 结果 |
|------|------|
| B站链接 → 下载合并 → 在线 ASR → 报告 | ✅ completed |
| PDF 导出（`/api/export?fmt=pdf`） | ✅ 8 页，含图片 |
| 文档库归档（`/api/library/videos`） | ✅ 1 视频 / V1 / 3 篇文章 |
| 衍生文体生成（量子速读） | ✅ generated |
| 衍生文体下载与 PDF 导出 | ✅ 1 页 PDF |

### 4.3 Windows 真机致命缺陷：窗口卡死（2026-09-11）

Windows 测试包一点击就卡死，终端刷满数百行
`Error while processing window.native.AccessibilityObject.Bounds.Empty.Empty...: maximum recursion depth exceeded`。

**根因（我们自己的代码）**：`desktop/shell.py` 的 `Bridge` 把 pywebview 的窗口对象存为**公开属性** `self.window`。
pywebview 注入 js_api 时会递归展开对象的**公开**属性（`webview/util.py::get_functions`，仅跳过下划线开头的名字），
于是走进原生窗口对象图：

```
window.native.AccessibilityObject.Bounds.Empty.Empty.Empty...
```

WinForms 的 `Rectangle.Empty` **每次访问返回新实例**，而 pywebview 靠 `id()` 去重，因此去重完全失效 → 无限递归。
`get_functions` 的 `except Exception` 会逐层捕获 `RecursionError` 并各打印一条，所以终端出现数百行重复错误；
同时注入线程卡死，`_pywebviewready` 事件永不触发，页面无法交互（表现为"卡住"）。

> macOS 走同一注入函数但不受影响：PyObjC 对象图不会无限自我引用。

| # | 问题 | 修复 |
|---|------|------|
| 13 | js_api 对象的公开属性指向原生窗口 → pywebview 无限递归 | 窗口引用改为私有属性 `_window`，并加注释说明约束 |

**验证**：用 pywebview 真实的 `get_functions` 逻辑 + 模拟 WinForms `Rectangle.Empty` 行为做对照测试：

| 场景 | 结果 |
|------|------|
| 修复后 `Bridge`（`_window` 指向原生窗口） | ✅ 稳定暴露 `pick_video/save_as/open_path`，无递归 |
| 回归旧写法（公开 `window`） | ✗ 复现 `RecursionError` |

> **通用教训**：凡是传给 `js_api` 的对象，其**公开**属性都会被 pywebview 递归内省；
> 任何指向原生/外部框架对象的引用都必须用下划线私有属性，或加 `_serializable = False`。

### 4.4 macOS 客户端报告无图：PyInstaller 漏收 vlm_handler（2026-09-11）

macOS 客户端处理完成后**报告里没有配图**，日志中出现唯一一条 ERROR：

```
ERROR - 无法初始化VLM处理器，跳过关键帧选择: No module named 'vlm_handler'
```

**两层根因**：

1. **`collect_submodules("backend")` 实际失效**：`backend/`、`backend/algorithm/`、`backend/devour/`
   都没有 `__init__.py`（隐式命名空间包），该调用只返回 `backend.api` 与 `backend.runtime`
   下的 5 个模块，`algorithm`/`devour` 一个都收不到。
   之前能运行只是碰巧——PyInstaller 顺着入口的静态 import 链收到了大部分模块。
2. **裸导入无法被静态分析**：`image_processor.py` 用 `from vlm_handler import VLMHandler`
   （函数内裸导入，依赖 `settings_store` 往 `sys.path` 插入 `backend/algorithm`）。
   冻结后代码都在 PYZ 归档里，该目录在磁盘上不存在，`sys.path` 插入无效。
   只被这种裸导入引用的 `vlm_handler` 因此被静默漏掉，而 `llm_handler` 因别处有
   `backend.algorithm.llm_handler` 的静态导入而侥幸收录——这正是"报告能生成但没图"的原因。

| # | 问题 | 修复 |
|---|------|------|
| 14 | 命名空间包导致 `collect_submodules("backend")` 失效 | spec 改为文件系统枚举 `backend/**/*.py` |
| 15 | 项目内裸导入在冻结包中失效（4 处） | 统一改为绝对导入 `backend.algorithm.*` |
| 16 | VLM 不可用时静默降级（用户只看到"处理成功"） | 错误信息改为可操作提示，明确说明"报告将不含配图" |

**验证**（macOS 冻结包，PATH 限制、无 torch）：

| 项 | 结果 |
|----|------|
| PYZ 收录 `backend.algorithm.*` | 16 → 20 个模块，含 `vlm_handler` |
| VLM 实际执行 | ✅ 每章 7-8 帧评分，选定 3 张关键帧 |
| 报告图片引用 | ✅ `![关键帧: ...](keyframes/01_xxx.jpg)` |
| 图片 HTTP 可访问 | ✅ `/static/<out_dir>/keyframes/<中文名>` → 200 image/jpeg |

> **通用教训**：spec 里不要对无 `__init__.py` 的目录用 `collect_submodules`；
> 项目内导入一律用绝对路径，避免依赖 `sys.path` 副作用（冻结后不成立）。

### 4.5 Intel (x86_64) 包：cryptography OpenSSL 符号冲突（2026-09-12）

新增 macOS Intel 构建时，x86_64 包在线 ASR 整体失败：

```
ERROR - [DashScope] ASR 处理失败: dlopen(.../cryptography/hazmat/bindings/_rust.abi3.so, 0x0002):
        symbol not found in flat namespace '_BIO_ADDR_free'
```

**根因（wheel 与解释器的链接方式不匹配，与 PyInstaller 无关）**：

- `cryptography` 的 macOS **x86_64 wheel（50.x）** 把 OpenSSL 符号（`_BIO_ADDR_free` 等）
  留作**未定义**，期望运行时从进程空间动态解析 —— 实测该 so 有 5+ 个未定义 OpenSSL 符号
- uv 提供的 x86_64 CPython **静态链接 OpenSSL 且不导出这些符号**（`nm -gU` 导出数为 0）
- arm64 wheel 则把 OpenSSL 静态解析进自身（未定义符号数 0），因此 arm64 包不受影响

由于 `dashscope`（在线 ASR）在运行时导入 `cryptography`，该错误只在"在线 ASR"链路触发。

| # | 问题 | 修复 |
|---|------|------|
| 17 | x86_64 cryptography wheel 依赖动态 OpenSSL，与静态链接的 Python 冲突 | 锁定 `cryptography==46.0.4`（实测两架构 wheel 均无未定义 OpenSSL 符号） |

**验证**：在 x86_64 环境逐一测试 50.0.1 / 46.0.4 / 45.0.7 / 44.0.1 / 43.0.3 的
`cryptography.hazmat.bindings._rust` 导入：仅 50.0.1 失败，46.0.4 及更早均正常。
锁定后 x86_64 冻结包端到端跑通：下载合并 → 在线 ASR → VLM 关键帧（4 张）→ 报告 → PDF（15 页），
图片经 `/static/<out_dir>/keyframes/<中文名>` 返回 200 image/jpeg。

> **通用教训**：为同一份代码构建多架构时，二进制 wheel 的链接方式可能不同 ——
> 不要假设"arm64 能跑就等于 x86_64 能跑"，每个架构都要实际构建并跑通链路。

---

## 5. 已实现的能力（对照方案条款）

- **4.2 启动与同源**：绑定 `127.0.0.1:0` 由 OS 分配端口，握手文件报告实际端口；前端由同一 FastAPI 托管；API/资源路由优先，SPA fallback 最后注册；未注册 `/api` 与缺失资源返回 404。
- **4.1 路径**：新增 `backend/runtime/paths.py`，`data_root` 优先级 `--data-dir` → `VIDEO_DEVOUR_DATA_DIR` → 平台默认目录 → 源码根（开发模式行为不变）。settings/tasks/uploads/output/subtitle_notes/document_library 已全部接入。
- **5.2 下载器**：ffmpeg/ffprobe 统一解析（环境变量 → 捆绑 `bin/` → PATH），已覆盖 ASR 音频提取、视频切片、抽帧、视频号校验。
- **6.2 桌面行为**：单实例文件锁、退出时回收进程树、原生文件选择/另存为/打开目录桥接。

---

## 6. 未完成 / 待验证（阻塞与缺口）

| 项 | 状态 | 说明 |
|----|------|------|
| Windows x64 真实构建 | ✅ | CI 通过（含启动 + health 冒烟）；业务链路待真机验证 |
| Windows 业务链路 | ⏳ | 需真机跑字幕速记 / 图文报告 |
| 中文/空格路径、拖放 | ⏳ | 需 GUI 手动验证（本机自动化受限） |
| 中文输入法、编辑器保存 | ⏳ | 属阶段 C，本次未覆盖 |
| 空闲内存/CPU 基线 | ⏳ | 方案要求阶段 A 后定预算 |
| 代码签名/公证 | ⏳ | 当前仅 ad-hoc 签名；正式分发需开发者证书 |
| ffmpeg 体积优化 | 💡 | 86MB 占比最大；可评估裁剪仅保留所需编码器的构建 |
| 匹配质量对照 | ⏳ | A2 遗留缺口，字符串匹配质量需人工抽查 |
| 睡眠/唤醒、网络切换 | ⏳ | 方案 6.2 要求，未测 |

---

## 7. 复现方式

```bash
# 1. 轻量依赖
uv venv .venv-lite --python 3.12
uv pip install --python .venv-lite/bin/python -r requirements-lite.txt pyinstaller pywebview

# 2. 捆绑二进制（静态构建）
./desktop/fetch_binaries.sh

# 3. 前端
cd frontend && npm run build && cd ..

# 4. 构建 .app
PYTHON=.venv-lite/bin/python ./desktop/build_mac.sh --sign

# 5. 运行
open desktop/dist/VideoDevour.app
# 内测被 Gatekeeper 拦截时：xattr -cr desktop/dist/VideoDevour.app
```

Windows：`desktop/build_win.ps1`（详见脚本头注释）。

---

## 8. 对后续阶段的输入

1. **B1 已部分完成**：`data_root` 重构已落地并被桌面壳使用；剩余工作是把旧数据迁移流程补上（方案 4.1 的显式迁移）。
2. **B2 部分完成**：同源托管、health 扩展已做；**本地会话 token 鉴权、CSRF、Host/Origin 校验尚未实现**（方案 4.2 明确要求，pywebview 官方也建议）。
3. **B3/B4 未开始**：任务仍是内存态 + JSON、无队列、无真实阶段事件、取消不贯通线程。
4. **D2 需要补**：`requirements-lite.txt` 目前是范围约束，正式发行前需 `uv lock` 生成锁文件（`mcp==1.17.0` 已锁）。
