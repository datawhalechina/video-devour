# VideoDevour Python 依赖（与 pyproject.toml 等价，供 pip 安装使用）
# 推荐优先使用：uv sync（按 uv.lock 精确锁定版本）
#
# 注意：LLM 框架的包名是 camel-ai（import 名为 camel）。
# PyPI 上的 `camel` 是一个无关的同名旧包，请勿安装。
camel-ai>=0.2.75
funasr>=1.2.7
moviepy>=2.2.1
sentence-transformers==5.1.0
numpy>=1.24.0,<2.3.0
opencv-contrib-python>=4.8.0
opencv-python>=4.8.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
PyYAML>=6.0.0
scikit-image>=0.20.0
soundfile>=0.12.0
torch>=2.0.0
modelscope[audio]>=1.9.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.2.1
dashscope>=1.20.0
openai>=1.40.0
yt-dlp[default]>=2024.8.6
requests>=2.31.0
pydantic>=2.0.0
aiohttp>=3.9.0
curl-cffi>=0.9.0
browser-cookie3>=1.4.0
bgutil-ytdlp-pot-provider>=2.0.0
# 报告导出 PDF（内置 CID 中文字体，无需外部字体/浏览器）
reportlab>=4.0.0
